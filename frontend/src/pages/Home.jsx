import { Link } from "react-router-dom";

const services = [["01", "3D Printing", "Fast, precise production for prototypes, technical parts, and small series."], ["02", "Product Design", "Turn an idea, sketch, or requirement into a manufacturable 3D model."], ["03", "Laser & Electronics", "New capabilities for engraved parts and intelligent products, coming soon."]];

const Home = () => <><section className="hero-home"><div><div className="eyebrow">Additive manufacturing / Tunisia</div><h1>MAKE THE NEXT<br />THING <span style={{ color: "var(--accent)" }}>REAL.</span></h1><p>Fabrinova3D transforms ideas into reliable physical products through industrial 3D printing, design, and rapid prototyping.</p><div className="hero-actions"><Link className="button" to="/devis">Start a project →</Link><Link className="button ghost" to="/realisations">View our work</Link></div></div></section><section className="stats">{[["24h", "Typical first reply"], ["0.1mm", "Layer precision"], ["10+", "Compatible materials"], ["100%", "Custom workflow"]].map(([number, label]) => <div className="stat" key={label}><strong>{number}</strong><span>{label}</span></div>)}</section><section className="content"><div className="section-heading"><div><div className="eyebrow">What we do</div><h2>BUILT FOR<br />YOUR BRIEF.</h2></div><p>Whether you need one prototype or a small production run, we select the right process and material for the job.</p></div><div className="grid three">{services.map(([number, title, text]) => <Link className="card service-card" to="/services" key={title}><span className="service-number">{number} / CAPABILITY</span><div><h3>{title}</h3><p className="muted">{text}</p></div><span className="mono" style={{ fontSize: 11 }}>EXPLORE →</span></Link>)}</div></section></>;

export default Home;
